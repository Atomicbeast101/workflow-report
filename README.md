# Gitea Workflow Report

Workflow template that is used by various DevOps pipelines in my self-hosted Gitea server. It sends push notifications via Pushover to notify developer the end result of the pipeline run.
